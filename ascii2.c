#include<stdio.h>
int main()
{
    char ch;
    if(ch >= 'A' && ch <= 'Z')
    else if(ch >= 'a' && ch <= 'z')
    else if(ch >= '0' && ch <='9')
    else if(is upper(ch))
    else if(is lower(ch))
    else if(is digit(ch))
    else
    (is special symbol)
    return 0;
}